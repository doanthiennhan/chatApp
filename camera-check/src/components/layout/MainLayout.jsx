/* import { Layout, App } from "antd";

const { Header, Content } = Layout;

const MainLayout = ({ title = "Camera Health Check", children }) => (
  <App>
    <Layout style={{ minHeight: "100vh", background: "#f5f6fa" }}>
      <Header
        style={{
          background: "#1677ff",
          color: "#fff",
          fontSize: 24,
          fontWeight: 700,
          letterSpacing: 1,
          display: "flex",
          alignItems: "center",
          height: 64,
          boxShadow: "0 2px 8px #e6f7ff",
          zIndex: 10,
          padding: "0 32px",
        }}
      >
        {title}
      </Header>
      <Content
        style={{
          padding: "16px",
          width: "100%",
          minHeight: "calc(100vh - 64px)",
          maxWidth: "100%",
          margin: 0,
        }}
      >
        {children}
      </Content>
    </Layout>
  </App>
);

export default MainLayout; */